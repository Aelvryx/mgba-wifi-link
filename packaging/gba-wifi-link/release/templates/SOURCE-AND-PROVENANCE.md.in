# Source and provenance

This release was built from `{{repository}}`.

- Tag: `{{tag}}`
- Annotated tag object: `{{tag_object}}`
- Peeled commit: `{{commit}}`
- Source date epoch: `{{source_date_epoch}}`

`BUILD-PROVENANCE.json` records the reproducible archive build, including the
exact source, validation jobs, toolchain, configuration, and two matching build
identities. The checksum manifests bind those records to the packaged files.
